{
  "name": "notification-system",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix notification_app_be && npm install --prefix notification_app_fe",
    "start": "concurrently \"npm start --prefix notification_app_be\" \"npm start --prefix notification_app_fe\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
